
var config = {
    canvas_width: 1280,
    canvas_height: 800,
    canvas_id: "game_area", // Specifica il div contenitore
    background_color: 0x000000,
    debug_mode: true,
    gravity_value: 300,
};

PP.game.create(config);
